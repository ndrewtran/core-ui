# G1.1 synthetic conformance fixtures

`platform-safety-fixtures.json` owns the two exact test-only fixture identities
required by `E-G1.1-06`. The file binds each fixture to one web profile and the
three applicable substrate assertions. It is proof input only: it owns no
component metadata, renderer behavior, support cell, or platform-safety
requirement identity.
