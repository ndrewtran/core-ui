interface:
  display_name: "Core UI Delivery"
  short_description: "Triage Core UI delivery through canonical owners"
  default_prompt: "Use $core-ui-delivery to map this Core UI task to canonical owners and the active milestone, then route one bounded implementation through focused deterministic checks, risk-based review, and the protected pull-request workflow; keep the root accountable and use fork_turns=none for every spawn."
