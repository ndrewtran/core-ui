# CheckboxGroup

Give the group a Core accessible name and set `name` when submitting a form. Core applies that name to each child checkbox, so checked values arrive as repeated entries under one FormData key.
