# CheckboxGroup

Give the group a Mux UI accessible name and set `name` when submitting a form. Mux UI applies that name to each child checkbox, so checked values arrive as repeated entries under one FormData key.
