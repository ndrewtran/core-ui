# DateRangePicker

Date values are Core ISO strings (`YYYY-MM-DD`). Set `startName` and `endName` for form submission; Core owns paired hidden inputs and resets them with the form. The calendar primitives remain internal to the React binding.
