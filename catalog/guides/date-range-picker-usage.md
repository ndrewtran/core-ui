# DateRangePicker

Date values are Mux UI ISO strings (`YYYY-MM-DD`). Set `startName` and `endName` for form submission; Mux UI owns paired hidden inputs and resets them with the form. The calendar primitives remain internal to the React binding.
