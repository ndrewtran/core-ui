# Decision navigation

Decisions record accepted choices that are not already fixed by higher
authority. Preserve decision numbers and history; amend or supersede a decision
instead of silently rewriting its meaning.

Repository decisions are checked through `pnpm check`.
