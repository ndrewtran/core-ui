# Application navigation

Applications consume enabled catalog and package surfaces. They are projections
or example hosts, never owners of component, token, example, or guidance truth.

Run the app-owned checks, then `pnpm check` for affected dependents.
